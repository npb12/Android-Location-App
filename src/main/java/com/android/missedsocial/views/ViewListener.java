package com.android.missedsocial.views;

/**
 * An interface to allow communication between a view and a controller.
 *
 * 
 */
public interface ViewListener {
	public void onViewChange(String message, Object what);
}